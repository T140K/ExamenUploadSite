@page "/Error/{statusCode}"
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error @Model.StatusCode</h1>
<h2 class="text-danger">The page you requested cannot be found</h2>
<hr />
<h3>If the page you tried to load still doesnt work after multiple tries contact darek1430143@gmail.com</h3>

@if (Model.ShowRequestId)
{
    @* <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p> *@
}